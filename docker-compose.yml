services:

  mongo:
    image: mongo:6
    container_name: mongo
    restart: always
    volumes:
      - mongo-data:/data/db
    networks:
      - hostel-network

  backend:
    image: samithspalan/deployment-backend:latest
    container_name: backend
    restart: always
    env_file:
      - .env
    depends_on:
      - mongo
    networks:
      - hostel-network

  frontend:
    image: samithspalan/deployment-frontend:latest
    container_name: frontend
    restart: always
    networks:
      - hostel-network

  nginx:
    image: nginx:alpine
    container_name: nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend
    networks:
      - hostel-network

networks:
  hostel-network:

volumes:
  mongo-data: